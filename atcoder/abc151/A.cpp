#include <iostream>
using namespace std;

#define rep(i, a, b) for (auto i = (a); i < (b); ++i)
#define trav(e, x) for (auto &e : x)
#define dbg(x) #x << ": " << x <<
#define sz(x) ((int)x.size())

signed main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);

	char c; cin >> c; cout << (char)(c + 1);
}